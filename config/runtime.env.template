# Copy this file to config/runtime.local.env (recommended) and fill your secrets.
# runtime.local.env is gitignored.

# Telegram bot identity and token
TELEGRAM_BOT_USERNAME=MioooooooooBot
TELEGRAM_BOT_KEY=
# Comma/space separated immutable Telegram numeric user IDs allowed to use private admin commands.
TELEGRAM_ADMIN_USER_IDS=

# Web admin settings. Keep WEBADMIN_HOST on 127.0.0.1 unless it is protected by HTTPS/reverse proxy.
WEBADMIN_HOST=127.0.0.1
WEBADMIN_PORT=8765
WEBADMIN_BASE_URL=http://127.0.0.1:8765
WEBADMIN_SESSION_TTL_SECONDS=43200
# HTTPS deployments always use secure cookies; keep 0 only for local HTTP development.
WEBADMIN_COOKIE_SECURE=0
WEBADMIN_SECRET_KEY=

# Azure OpenAI settings (used when provider is azure)
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_API_VERSION=2024-04-01-preview
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-5-mini

# Ark/Volcengine settings (used when provider is ark)
ARK_API_ENDPOINT=https://ark.cn-beijing.volces.com/api/v3/chat/completions
ARK_API_KEY=
ARK_MODEL=doubao-seed-1-8-251228
ARK_VISION_MODEL=doubao-seed-1-6-251015

# ZAN OpenAI-compatible settings (used when provider is zan)
ZAN_OPENAI_BASE_URL=https://ai.zan.top/v1
ZAN_API_KEY=
ZAN_MODEL=qwen3.7-plus
# Leave empty to use ZAN_MODEL for image and sticker understanding.
ZAN_VISION_MODEL=qwen3.7-plus

# Provider selection
LLM_PROVIDER=zan
LLM_ENABLE_THINKING=0
# Enable only while diagnosing provider-side prefix-cache behavior. No prompt content is logged.
LLM_LOG_CACHE_HEADERS=0

# Optional local LLM (used when provider is ollama)
OLLAMA_ENDPOINT=http://100.69.97.8:11434
OLLAMA_MODEL=gpt-oss:20b

# SQLite database path (persistent by default)
DB_FILE=data/message_history.db

# Message history controls
MESSAGE_REVIEW_BACK=80
RAG_TOP_K=12
# The ambient reply probe uses a short context and no retrieval.
PROBE_MESSAGE_REVIEW_BACK=12
# Keep this many oldest messages as a stable prefix once the recent-history window rolls.
PROMPT_CACHE_ANCHOR_MESSAGES=12

# Public-media and document resource limits.
MAX_RENDER_DOCUMENT_BYTES=10485760
MAX_MEDIA_LINKS_PER_MESSAGE=4
MEDIA_PROCESSING_CONCURRENCY=2
MAX_TWITTER_MEDIA_BYTES=52428800
MAX_TWITTER_MEDIA_ITEMS=10

# Sticker reply controls
STICKER_REPLY_ENABLED=1
STICKER_REPLY_CANDIDATE_LIMIT=12
STICKER_REPLY_COOLDOWN_MINUTES=30
STICKER_REPLY_RATIO_WINDOW=20
AMBIENT_REPLY_COOLDOWN_SECONDS=180
SUPPORT_STATE_TTL_SECONDS=600

# Personal memory candidate extraction controls
MEMORY_CANDIDATE_EXTRACTION_ENABLED=1
MEMORY_CANDIDATE_AUTO_REFRESH_COUNT=3
PERSONAL_MEMORY_CONTEXT_MAX_CHARS=1800
PERSONAL_MEMORY_CANDIDATE_FACTS=24

# Group-scoped global memory controls
GLOBAL_MEMORY_CONTEXT_MAX_CHARS=1000
GLOBAL_MEMORY_CANDIDATE_FACTS=32

# RAG embedding settings
EMBED_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
# background (default), blocking, or disabled. Use the CLI for manual rebuilds:
# miobot-rag reindex
RAG_REINDEX_ON_STARTUP=background
